/// Matches `pubspec.yaml` `version` (semver before `+build`).
const String trampAppVersion = '0.1.0';

/// First product year shown in About (not the current calendar year).
const int trampCopyrightYear = 2026;

const String trampCompanyName = 'Proxima Magnifica';

/// The pledge set beside the © year on the About window's maker's plate.
///
/// Deliberately *not* the company name: the plate already sets that in chrome
/// type directly above, and repeating it under the © read as a doubled logo.
/// The slot is worth more as a promise than as an echo.
const String trampFreePromise = 'Free Forever';

const String trampWebsiteUrl = 'https://tramp.music';

/// Backronym under the wordmark in About. Word initials must spell TRAMP —
/// the window lights them to make the joke land.
const String trampBackronym = 'The Ridiculously Attractive Music Player';

/// One-line product description shown in About.
const String trampTagline =
    'Local files, honest tags, and chrome you can feel.';
