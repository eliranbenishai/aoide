# Write the v1 product spec into the repo

Type: task
Status: open
Blocked by: 02, 03, 04

## Question

With stack, remaining product edges, and UI direction decided, write the **Tramp v1 product spec** into this repo at the agreed path.

The spec should capture: platforms; local playback; scalable UI + app chrome; formats; playlist manager; core player chrome; window model; locked stack assumptions; non-goals (classic skins, library, etc.). This task is done when the file exists and is complete enough to plan implementation — not when Tramp is built.
