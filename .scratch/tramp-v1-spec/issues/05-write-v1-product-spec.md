# Write the v1 product spec into the repo

Type: task
Status: resolved
Blocked by: 02, 03, 04

## Question

With stack, remaining product edges, and UI direction decided, write the **Tramp v1 product spec** to [`docs/tramp-v1-spec.md`](../../../docs/tramp-v1-spec.md).

The spec should capture: platforms; local playback; scalable UI + app chrome; formats; playlist manager; core player chrome; window model; locked stack assumptions; distribution/file associations/a11y decisions; non-goals (classic skins, library, gapless/crossfade, store listings, etc.). This task is done when the file exists and is complete enough to plan implementation — not when Tramp is built.

## Answer

Wrote [`docs/tramp-v1-spec.md`](../../../docs/tramp-v1-spec.md) — v1 product requirements consolidated from wayfinder decisions (stack, edges, UI direction W, non-goals). Ready to plan implementation; Tramp app not built in this task.
