{
  // Tramp: prefer Auto-review run mode (Settings > Agents > Approvals & Execution).
  // Empty mcpAllowlist = no MCP tools auto-run; classifier steered to confirm MCP/Fetch.
  "mcpAllowlist": [],
  "autoRun": {
    "allow_instructions": [
      "Allow shell commands that stay in this workspace for build, test, git, Flutter, Dart, and other local project work.",
      "Allow sandboxed terminal commands that only affect the current workspace."
    ],
    "block_instructions": [
      "Always require approval for MCP tool calls.",
      "Always require approval for WebFetch and WebSearch.",
      "Always require approval for network fetches and remote HTTP requests from agent tools."
    ]
  }
}
