# Assets

Historical PNG-graphite request list — retired when chrome became code-constructed from the mockup.

Current brand files:

| Asset | Path |
|-------|------|
| Logo (vector source) | `assets/branding/logo.svg` |
| App icon (PNG) | `assets/branding/app_icon.png` |
| Logo (PNG) | `assets/branding/logo.png` |
| Maker's plate mark | `assets/branding/proxima_mark.png` |
| Windows installer / EXE icon | `packaging/windows/app_icon.ico` |
| macOS bundle icon | `packaging/macos/tramp.icns` |
| Linux hicolor icons | `packaging/linux/icons/hicolor/` |

Regenerate rasters from the SVG with `tool/generate_app_icons.sh`.
