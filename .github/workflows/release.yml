name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

env:
  # Override with repo variable MSIX_PUBLISHER after Partner Center identity exists.
  MSIX_PUBLISHER_DEFAULT: "CN=Proxima Magnifica"
  MSIX_IDENTITY_DEFAULT: "ProximaMagnifica.aoide"

  # The Qt pin lives in QT_VERSION (x.y.z). export-qt-pin.sh writes QT_VERSION
  # and QT_RUNTIME (x.y) into GITHUB_ENV. That is the same line as
  # org.kde.Platform and as local ./tool/fetch_qt.sh.

jobs:
  test:
    name: Test (Linux Qt)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Qt pin
        run: bash tool/export-qt-pin.sh >> "$GITHUB_ENV"
      - name: Build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake ninja-build g++ pkg-config appstream \
            libgl1-mesa-dev libx11-dev libmpv-dev \
            libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-xkb1 \
            libxkbcommon-x11-0 libwayland-client0 libwayland-cursor0 libwayland-egl1
      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          arch: linux_gcc_64
          cache: true
          archives: qtbase qtwayland icu
      # This job fronts every packaging job, so a metainfo file that would install
      # under the app ID instead of the name fails before anything is built.
      # --check-urls only here, not in ci.yml: this is the path that publishes, and
      # a listing advertising a screenshot nobody can load is worse than one with
      # none. It runs on every dispatch rather than only on a tag, so it is not
      # first exercised during a real release.
      - name: AppStream metainfo
        run: bash tool/check-metainfo.sh --check-urls
      - name: Tag matches VERSION
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          chmod +x tool/version.sh
          eval "$(./tool/version.sh)"
          tag="${GITHUB_REF_NAME#v}"
          if [[ "$tag" != "$version" ]]; then
            echo "Tag $GITHUB_REF_NAME does not match VERSION $version" >&2
            exit 1
          fi
      - name: Configure and test
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release
          ctest --test-dir build -C Release --output-on-failure
      - name: Paint budget
        run: QT_QPA_PLATFORM=offscreen ./build/aoide --bench-chrome

  windows:
    name: Windows EXE + MSIX
    needs: test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Qt pin
        shell: bash
        run: bash tool/export-qt-pin.sh >> "$GITHUB_ENV"
      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          cache: true
          archives: qtbase
      - name: Fetch full libmpv
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File tool/fetch_full_libmpv.ps1
      - name: Configure and build
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release
          ctest --test-dir build -C Release --output-on-failure
        shell: bash
      - name: Stage
        shell: pwsh
        run: powershell -ExecutionPolicy Bypass -File packaging/windows/stage.ps1
      # Run what we are about to wrap in an installer. ctest above ran the build
      # tree; this is the only step that proves the staged tree has its Qt DLLs,
      # its libmpv and its assets. The support dir is not redirected here —
      # XDG_DATA_HOME only binds on Linux — but the runner is thrown away.
      - name: Smoke-test the staged EXE
        shell: bash
        run: |
          set -euo pipefail
          # Plugins are not found through PATH, so stripping Qt's bin below does
          # nothing for them: install-qt-action exports QT_PLUGIN_PATH, and with it
          # set the staged tree loads the runner's platform plugins and a stage
          # carrying none of its own still starts. Same reason the Linux job does
          # this — see stage.ps1 for what makes the stage self-sufficient.
          unset QT_PLUGIN_PATH QML2_IMPORT_PATH
          # Windows searches the exe's own directory first but PATH after it, and
          # install-qt-action puts Qt's bin on PATH. Drop that entry, or a stage
          # windeployqt left a DLL out of would still start.
          if [ -n "${QT_ROOT_DIR:-}" ]; then
            qt_bin="$(cygpath -u "$QT_ROOT_DIR")/bin"
            PATH="$(printf '%s\n' "$PATH" | tr ':' '\n' | grep -vxF "$qt_bin" | paste -sd ':' -)"
            export PATH
          fi
          QT_QPA_PLATFORM=offscreen build/windows/stage/aoide.exe --bench-chrome
          AOIDE_AUTO_QUIT=1 build/windows/stage/aoide.exe
      - name: Inno Setup installer
        shell: pwsh
        run: |
          choco install innosetup --no-progress -y
          powershell -ExecutionPolicy Bypass -File tool/fetch_vc_redist.ps1
          $ver = (Get-Content VERSION -First 1).Trim()
          $iscc = Get-ChildItem -Path "${env:ProgramFiles(x86)}\Inno Setup *","${env:ProgramFiles}\Inno Setup *" -Filter ISCC.exe -Recurse -ErrorAction SilentlyContinue |
            Select-Object -First 1
          if (-not $iscc) { throw 'ISCC.exe not found' }
          & $iscc.FullName "/DMyAppVersion=$ver" packaging\windows\aoide.iss
      - name: MSIX (unsigned, Store re-signs)
        shell: pwsh
        env:
          MSIX_PUBLISHER: ${{ vars.MSIX_PUBLISHER }}
          MSIX_IDENTITY_NAME: ${{ vars.MSIX_IDENTITY_NAME }}
        run: |
          $ver = (Get-Content VERSION -First 1).Trim()
          $parts = $ver.Split('.'); while ($parts.Count -lt 3) { $parts += '0' }
          $msix = "$($parts[0]).$($parts[1]).$($parts[2]).0"
          $pub = if ($env:MSIX_PUBLISHER) { $env:MSIX_PUBLISHER } else { $env:MSIX_PUBLISHER_DEFAULT }
          $id = if ($env:MSIX_IDENTITY_NAME) { $env:MSIX_IDENTITY_NAME } else { $env:MSIX_IDENTITY_DEFAULT }
          & packaging\windows\make_msix.ps1 -Version $msix -Publisher $pub -IdentityName $id
      - uses: actions/upload-artifact@v6
        with:
          name: windows
          # Default is to warn on an empty glob, which turns a packaging step that
          # silently produced nothing into a green job and a release missing an
          # installer.
          if-no-files-found: error
          path: |
            build/windows/installer/*.exe
            build/windows/msix/*.msix

  linux:
    name: Linux AppImage + tarball
    needs: test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Qt pin
        run: bash tool/export-qt-pin.sh >> "$GITHUB_ENV"
      - name: Linux build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake ninja-build g++ pkg-config \
            libgl1-mesa-dev libx11-dev libmpv-dev libmpv2 patchelf \
            libxcb-cursor0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 \
            libxcb-randr0 libxcb-render-util0 libxcb-shape0 libxcb-xkb1 \
            libxkbcommon-x11-0 libwayland-client0 libwayland-cursor0 libwayland-egl1
      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          arch: linux_gcc_64
          cache: true
          archives: qtbase qtwayland icu
      - name: Stage full libmpv into the tree
        run: |
          chmod +x tool/*.sh packaging/linux/*.sh
          ./tool/stage_linux_libmpv.sh
      - name: Configure, build, stage
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release
          ctest --test-dir build -C Release --output-on-failure
          QT_QPA_PLATFORM=offscreen ./build/aoide --bench-chrome
          ./packaging/linux/stage_bundle.sh
      - name: AppImage + tarball
        run: |
          ./packaging/linux/make_appimage.sh
          eval "$(./tool/version.sh)"
          tar -C build/linux -czf \
            "build/linux/Aoide-${version}-linux-x86_64.tar.gz" bundle
      # Run the things we are about to publish. ctest above ran the build tree,
      # which still resolves Qt and libmpv from the runner; only these runs prove
      # the artifacts carry their own. The tarball is extracted somewhere else
      # first, because relocating is the whole claim.
      - name: Smoke-test the artifacts
        run: |
          set -euo pipefail
          # Take the runner's Qt out of the environment first. install-qt-action
          # exports LD_LIBRARY_PATH, and with it set an artifact missing its own
          # Qt still starts — the check would pass for the wrong reason.
          unset LD_LIBRARY_PATH QT_PLUGIN_PATH QT_QPA_PLATFORM_PLUGIN_PATH
          export QT_QPA_PLATFORM=offscreen APPIMAGE_EXTRACT_AND_RUN=1
          appimage="$(ls build/linux/Aoide-*-linux-x86_64.AppImage)"
          tarball="$(ls build/linux/Aoide-*-linux-x86_64.tar.gz)"

          build/linux/bundle/aoide --bench-chrome
          XDG_DATA_HOME="$(mktemp -d)" AOIDE_AUTO_QUIT=1 build/linux/bundle/aoide

          "$appimage" --bench-chrome
          XDG_DATA_HOME="$(mktemp -d)" AOIDE_AUTO_QUIT=1 "$appimage"

          extracted="$(mktemp -d)"
          tar -C "$extracted" -xzf "$tarball"
          "$extracted/bundle/aoide" --bench-chrome
          XDG_DATA_HOME="$(mktemp -d)" AOIDE_AUTO_QUIT=1 "$extracted/bundle/aoide"
      - uses: actions/upload-artifact@v6
        with:
          name: linux
          if-no-files-found: error
          path: |
            build/linux/Aoide-*-linux-x86_64.AppImage
            build/linux/Aoide-*-linux-x86_64.tar.gz

  # This packaging lane (CMake bundle, stage_app.sh, notarize.sh, make_dmg.sh)
  # has run on a Mac: CI has built and notarized a DMG, and one has been
  # installed and played on a MacBook. That is one machine, and nothing here
  # launches the *installed* app. continue-on-error so a failure here cannot
  # fail a Windows/Linux release. github-release lists this job in needs so a
  # produced DMG is flattened, but does not require
  # needs.macos.result == success.
  macos:
    name: macOS universal DMG
    needs: test
    runs-on: macos-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - name: Qt pin
        run: bash tool/export-qt-pin.sh >> "$GITHUB_ENV"
      # fetch_qt.sh exits 1 on Darwin. Same action and pin as the Windows job;
      # host/arch request the official mac desktop kit (clang_64 is the
      # universal Qt 6.5+ layout).
      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          host: mac
          target: desktop
          arch: clang_64
          cache: true
          # macdeployqt lives in qttools. windeployqt is in qtbase, which is why
          # the Windows job can slice to qtbase alone.
          archives: qtbase qttools
      - name: Fetch full libmpv
        run: |
          chmod +x tool/*.sh packaging/macos/*.sh
          ./tool/fetch_full_libmpv.sh
      - name: Configure and build
        run: |
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release
          ctest --test-dir build -C Release --output-on-failure
      - name: Stage
        env:
          AOIDE_BUILD_DIR: ${{ github.workspace }}/build
        run: ./packaging/macos/stage_app.sh
      # Run the staged bundle before it is signed. ctest above ran the build
      # tree, which still resolves Qt and libmpv from the runner; only this
      # run proves the stage carries its own. The support dir is not redirected
      # here — XDG_DATA_HOME only binds on Linux; macOS uses
      # ~/Library/Application Support — but the runner is thrown away.
      # notarize.sh then clears extended attributes with xattr -cr before
      # signing, so this run cannot spoil the signature. It is blocking, as it is
      # on Windows and Linux: this job is already allowed to fail, so a bundle
      # that will not start costs this release its DMG rather than shipping one
      # nobody ran. The clock is there because a wedged start would otherwise
      # hold the job for six hours.
      - name: Smoke-test the staged app
        timeout-minutes: 5
        run: |
          set -euo pipefail
          # Take the runner's Qt out of the environment first. install-qt-action
          # exports QT_PLUGIN_PATH, DYLD_FRAMEWORK_PATH and DYLD_LIBRARY_PATH,
          # and with those set a stage missing its own Qt still starts — the
          # check would pass for the wrong reason.
          unset QT_PLUGIN_PATH DYLD_FRAMEWORK_PATH DYLD_LIBRARY_PATH
          export QT_QPA_PLATFORM=offscreen
          build/macos/stage/Aoide.app/Contents/MacOS/Aoide --bench-chrome
          AOIDE_AUTO_QUIT=1 build/macos/stage/Aoide.app/Contents/MacOS/Aoide
      - name: Sign, notarize, wrap DMG
        env:
          AOIDE_MAC_APP: ${{ github.workspace }}/build/macos/stage/Aoide.app
          MACOS_CERTIFICATE_BASE64: ${{ secrets.MACOS_CERTIFICATE_BASE64 }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          APPLE_API_KEY_BASE64: ${{ secrets.APPLE_API_KEY_BASE64 }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          eval "$(./tool/version.sh)"
          export AOIDE_MAC_DMG="${GITHUB_WORKSPACE}/build/macos/Aoide-${version}-macos-universal.dmg"
          # notarize.sh no-ops (exit 0) when the certificate secrets are unset
          # and does not write a DMG. When they are set it signs the app, then
          # calls make_dmg.sh itself so the sealed bundle is what gets wrapped.
          # Calling make_dmg.sh unconditionally as well would rebuild the image.
          ./packaging/macos/notarize.sh "$AOIDE_MAC_DMG"
          if [[ ! -f "$AOIDE_MAC_DMG" ]]; then
            ./packaging/macos/make_dmg.sh
          fi
      - uses: actions/upload-artifact@v6
        with:
          name: macos
          if-no-files-found: error
          path: build/macos/Aoide-*-macos-universal.dmg

  flatpak:
    name: Linux Flatpak bundle
    needs: test
    runs-on: ubuntu-24.04
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - name: Qt pin
        run: bash tool/export-qt-pin.sh >> "$GITHUB_ENV"
      # Built here rather than unpacked from the linux job's tarball. That
      # tarball carries Qt, because a tarball has to; this runs on
      # org.kde.Platform, which is a Qt runtime, and taking the tarball is what
      # used to put a second Qt inside the sandbox. Same staging script as the
      # tarball and the AppImage, with --no-qt — see make_flatpak.sh.
      - name: Linux build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake ninja-build g++ pkg-config \
            libgl1-mesa-dev libx11-dev libmpv-dev libmpv2 patchelf
      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          arch: linux_gcc_64
          cache: true
          archives: qtbase qtwayland icu
      - name: Configure and build
        run: |
          chmod +x tool/*.sh packaging/linux/*.sh
          ./tool/stage_linux_libmpv.sh
          cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release
      - name: Install flatpak-builder
        run: |
          sudo apt-get update
          # elfutils is only a Recommends of the Ubuntu package. Without it,
          # flatpak-builder dies after the module install: "eu-strip" not found.
          sudo apt-get install -y --no-install-recommends \
            flatpak flatpak-builder elfutils
          sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install -y flathub \
            "org.kde.Platform//${QT_RUNTIME}" "org.kde.Sdk//${QT_RUNTIME}"
      - name: Build .flatpak
        run: ./packaging/linux/make_flatpak.sh
      # A second Qt inside a Qt runtime is invisible in a bundle that otherwise
      # works, so assert it rather than trusting the flag stayed wired up.
      - name: Flatpak staging carries no Qt
        run: |
          set -euo pipefail
          stage=build/linux/flatpak-bundle
          if find "$stage" -name 'libQt6*' | grep .; then
            echo "^ Qt in the Flatpak staging: stage_bundle.sh --no-qt regressed" >&2
            exit 1
          fi
          test ! -e "$stage/plugins"
          test ! -e "$stage/qt.conf"
          find "$stage/lib" -name 'libmpv.so*' | grep .
      - name: Smoke-test the .flatpak
        run: |
          set -euo pipefail
          sudo flatpak install -y --noninteractive \
            "$(ls build/linux/Aoide-*-linux-x86_64.flatpak)"
          # The name a listener actually sees. flatpak reads it from the AppStream
          # data composed out of /app/share/metainfo, and falls back to printing the
          # app ID when that is missing — which is silent everywhere else in this
          # build, since flatpak-builder skips its compose step without a word.
          name="$(flatpak list --app --columns=application,name \
            | awk -F'\t' '$1 == "com.proximamagnifica.aoide" { print $2 }')"
          if [ "$name" != "Aoide" ]; then
            echo "installed name is '$name', expected 'Aoide'" >&2
            echo "the bundle is missing its AppStream metainfo" >&2
            exit 1
          fi
          # System install is fine; `sudo flatpak run` is not. Newer flatpak
          # refuses that form so the sandbox does not inherit sudo's env.
          flatpak run --env=QT_QPA_PLATFORM=offscreen \
            com.proximamagnifica.aoide --bench-chrome
          flatpak run --env=QT_QPA_PLATFORM=offscreen --env=AOIDE_AUTO_QUIT=1 \
            com.proximamagnifica.aoide
      - uses: actions/upload-artifact@v6
        with:
          name: linux-flatpak
          if-no-files-found: error
          path: build/linux/Aoide-*-linux-x86_64.flatpak

  github-release:
    name: Assemble downloads
    # Not gated on the tag as a whole. Every run of this workflow so far has been a
    # workflow_dispatch, so a tag-gated job meant the download, the flatten glob and
    # the completeness check below had never run once — the first real tag would have
    # been their first execution. Assembling is now unconditional and only the publish
    # step is tag-only.
    #
    # flatpak and macos are in needs so the flatten glob can actually see a
    # .flatpak / .dmg. Both jobs are continue-on-error, so waiting on them does
    # not make them required — only windows and linux must succeed.
    needs: [windows, linux, flatpak, macos]
    if: >
      !cancelled() &&
      needs.windows.result == 'success' &&
      needs.linux.result == 'success'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v7
        with:
          path: dist
      - name: Flatten artifacts
        run: |
          set -euo pipefail
          mkdir -p upload
          find dist -type f \( -name '*.exe' -o -name '*.msix' -o -name '*.AppImage' \
            -o -name '*.tar.gz' -o -name '*.flatpak' -o -name '*.dmg' \) \
            -exec cp -v {} upload/ \;
          ls -la upload
      - name: Every promised download is here
        run: |
          set -euo pipefail
          # A release that is short one asset ships fewer platforms than the download
          # page offers, and nothing upstream of here says so. The Flatpak and the
          # macOS DMG are absent from this list on purpose: those jobs are allowed
          # to fail.
          missing=0
          for pattern in '*.exe' '*.msix' '*.AppImage' '*.tar.gz'; do
            if ! ls upload/$pattern >/dev/null 2>&1; then
              echo "nothing matching $pattern in upload/" >&2
              missing=1
            fi
          done
          exit "$missing"
      - uses: softprops/action-gh-release@v3
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: upload/*
          generate_release_notes: true
          draft: false
          body: |
            Source: this tag. Product downloads: https://aoide.music

            Windows Store and Flathub listings are submitted separately from these bits.
            macOS DMG is 1.1. It is unsigned until Developer ID secrets are set.
            CI builds and smoke-starts one on every run, and one has been
            installed and played on a MacBook — a single machine, not a channel.
